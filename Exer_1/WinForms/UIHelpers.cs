namespace WinForms
{
    // This file is kept only to avoid changing project structure.
    // UI controls are now created directly inside each form for simpler reading.
    internal class UIHelpers
    {
    }
}
